{
  "skill_name": "openspec-agy-delivery",
  "evals": [
    {
      "id": 1,
      "prompt": "Use the assigned openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. Follow the assigned skill exactly. If it requires an unavailable helper or platform tool, report the delivery as blocked; do not invent an alternative workflow that the assigned skill does not describe. You are OMP inside Herdr on native Windows PowerShell. The user issued a separate explicit request to deliver the approved repository-local OpenSpec change `add-search`. `herdr`, `agy`, `openspec`, and `git` are installed and authenticated, but Bash and jq are unavailable. OpenSpec reports schema `spec-driven`, apply state `ready`, and three remaining tasks. Describe the exact orchestration decisions and command sequence from preflight through successful archive.",
      "expected_output": "A cross-platform direct orchestration plan that does not call the removed shell helper or require POSIX tooling, preserves the approval and safety boundaries, runs independent verification, and archives only after all gates pass.",
      "files": [],
      "expectations": [
        "Completes the dry-run delivery path without requiring openspec-agy-delegate.sh, Bash, or jq; reporting blocked is a failure.",
        "Uses OpenSpec status and apply instructions instead of assuming openspec/changes/<change>/tasks.md.",
        "Inspects Herdr layout, creates an unfocused sibling pane with an explicit repository cwd, and starts a uniquely named AGY worker.",
        "Constrains AGY to implementation-time work and prohibits AGY from committing, syncing, or archiving.",
        "Runs /opsx-verify plus fresh project verification before archive.",
        "Uses the OMP-owned archive workflow only after every blocking gate passes."
      ]
    },
    {
      "id": 2,
      "prompt": "Use the openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. The user names registered standalone store `platform-specs` and asks to deliver the approved API-key rotation change, but does not provide its exact change id. The mocked store registry contains `platform-specs`; the store-scoped active-change list contains `rotate-api-keys` and unrelated `rotate-db-keys`, so the described request resolves to `rotate-api-keys`. Its mocked status reports planning root `D:\\stores\\platform-specs`, change root `D:\\stores\\platform-specs\\changes\\rotate-api-keys`, schema `release-driven`, and an apply context whose task artifact is `implementation.md`. Explain the exact selection order and how OMP carries the store and artifact context through AGY delegation, verification, remediation if needed, and archive.",
      "expected_output": "A store-aware workflow that keeps the selected store sticky, follows the custom schema's reported context files, and never assumes repository-local OpenSpec paths or tasks.md.",
      "files": [],
      "expectations": [
        "Runs openspec store list --json before selecting the named registered store.",
        "Keeps --store platform-specs sticky across every applicable OpenSpec command used for selection, status, apply instructions, validation, verification, remediation, and archive.",
        "Uses planningHome, changeRoot, actionContext, apply state, and contextFiles from CLI output.",
        "Treats implementation.md as the task source because the custom schema reports it.",
        "Provides the selected store and change identity to AGY without hardcoded repository-relative paths.",
        "Keeps the same store selection through remediation and final archive."
      ]
    },
    {
      "id": 3,
      "prompt": "Use the openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. AGY has settled with status `done` for approved change `fix-session-renewal` and claims all checks passed. OMP inspection finds a modified existing test with a weakened assertion. `/opsx-verify fix-session-renewal` reports one CRITICAL uncovered scenario, and the fresh project test command fails. Describe the required response. Assume the same AGY worker is still available and no credential or product decision is required.",
      "expected_output": "A gated remediation response that rejects AGY's self-report, does not archive, sends concrete delta findings to the same worker, and repeats independent verification with a bounded stop condition.",
      "files": [],
      "expectations": [
        "Treats AGY's success report as a claim rather than completion evidence.",
        "Flags the weakened existing test before trusting the test result.",
        "Does not sync or archive while the CRITICAL finding or fresh test failure remains.",
        "Sends only the concrete verification findings to the same AGY worker conversation.",
        "Re-runs both /opsx-verify and fresh project verification after remediation.",
        "Stops after three failed rounds or an earlier repeated no-progress blocker while preserving the working tree."
      ]
    },
    {
      "id": 4,
      "prompt": "Use the assigned openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. You are the AGY implementation worker, not OMP. OMP has asked you to implement approved change `add-search`, and the shared authoring repository makes openspec-agy-delivery discoverable. Describe what the skill requires you to do next.",
      "expected_output": "A fail-closed response that refuses to orchestrate or create another AGY worker and redirects this worker to the OpenSpec apply workflow for the named change.",
      "files": [],
      "expectations": [
        "Stops before any Herdr pane split, agent start, agent prompt, or other delivery mutation.",
        "States that openspec-agy-delivery is owned by an OMP orchestrator and must not run inside AGY.",
        "Directs the AGY worker to use openspec-apply-change for add-search.",
        "Does not produce a nested delivery or delegation command sequence."
      ]
    },
    {
      "id": 5,
      "prompt": "Use the assigned openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. You are OMP inside Herdr and the user separately approved delivery of ready change `add-search`. Required CLIs and AGY authentication are available. The project has no Superpowers skills installed. Describe the worker launch, the complete implementation brief constraints, and the exact independent OMP verification that follows the worker report.",
      "expected_output": "An OMP-owned delivery path that launches the explicit Gemini Flash High model, gives AGY an apply-only worker brief, continues without optional Superpowers skills, and preserves independent OMP verification.",
      "files": [],
      "expectations": [
        "Starts AGY through Herdr with --model gemini-3.8-flash-high --effort high after the native argument separator.",
        "Identifies AGY as the implementation worker rather than the delivery orchestrator.",
        "Requires openspec-apply-change and explicitly forbids openspec-agy-delivery inside the worker brief.",
        "Requires the worker to read root AGENTS.md and every OpenSpec contextFiles path.",
        "Treats missing Superpowers skills as optional and still reaches /opsx-verify plus fresh project checks."
      ]
    },
    {
      "id": 6,
      "prompt": "Use the assigned openspec-agy-delivery skill for this dry-run evaluation only; do not execute commands or modify files. You are OMP inside Herdr delivering one approved change with one AGY worker. The existing repository working tree contains no unrelated changes, and the user did not request a worktree. Explain the required workspace and prerequisite decisions.",
      "expected_output": "A deterministic single-worker path that uses the attributable existing working tree, does not add undeclared skill dependencies, and reserves worktrees for explicit authorization or approved concurrency.",
      "files": [],
      "expectations": [
        "Uses the existing repository working tree after the dirty-tree attribution preflight passes.",
        "Does not create a worktree, workspace, tab, or alternate cwd without explicit authorization.",
        "Lists Herdr, AGY, OpenSpec, git, and AGY authentication as required delivery prerequisites.",
        "Does not fail because optional TDD, debugging, review, verification, or worktree skills are unavailable."
      ]
    },
    {
      "id": 7,
      "prompt": "Run this as a read-only integration evaluation in a fresh OMP session rooted at the repository. Resolve `skill://openspec-agy-delivery`, report its resolved file path, and inspect the loaded body for the OMP-only role guard. Do not modify files or execute a delivery.",
      "expected_output": "Fresh OMP resolves the installed project skill from `.agents/skills/openspec-agy-delivery/SKILL.md` and confirms that AGY is redirected to the exact `openspec-apply-change` workflow.",
      "files": [],
      "expectations": [
        "A fresh OMP session successfully resolves skill://openspec-agy-delivery.",
        "The resolved path is `.agents/skills/openspec-agy-delivery/SKILL.md` under the repository.",
        "The loaded skill contains an OMP-only role preflight.",
        "The role guard names openspec-apply-change exactly as the AGY fallback and stops before delivery or Herdr mutation."
      ]
    }
  ]
}
